Coloque aqui suas instruções ou comentários que ache útil/relevantes para possamos ler 
quando estivermos avaliando a sua solução

comando para executar o one-jar (prompt de comando):

java -jar dojodos-1.0.0.one-jar.jar "path do log"

para executar a aplicação web é necessário fazer o deploy em um servidor
tomcat7 ou superior. O contexto da aplicação é servidor:porta/DojoAPP/